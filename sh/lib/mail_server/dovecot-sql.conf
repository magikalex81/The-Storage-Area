driver = mysql
#default_pass_scheme = MD5
connect = host=localhost dbname=postfix user=postfix password=*****
user_query = SELECT concat('/home/virtual/', username) AS home, concat('sdbox:/home/virtual/',username) AS mail, concat('*:bytes=', quota) AS quota_rule FROM mailbox WHERE username = '%u' AND active = '1'
password_query = SELECT username as user, password FROM mailbox WHERE username = '%u' AND active = '1'
# For using doveadm -A:
iterate_query = SELECT username FROM mailbox WHERE active = '1'
